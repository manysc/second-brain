import { AppShell } from "@/components/AppShell";
import { getMeeting } from "@/lib/api";
export default async function Impact() { const m = await getMeeting(); return <AppShell><div className="page-head"><div><p className="eyebrow">Growth / Impact journal</p><h1>Evidence of impact</h1><p className="lede">A review-ready index of observable contributions, with no fabricated outcomes.</p></div></div><section className="impact-block"><p className="eyebrow">Q3 / Technical leadership</p><h2>MS-PS 1-on-1</h2><div className="impact-stats"><span><b>{m.items.filter(i=>i.type==='DECISION').length}</b> decisions</span><span><b>{m.items.filter(i=>i.type==='ACTION').length}</b> actions</span><span><b>{m.items.filter(i=>i.type==='QUESTION').length}</b> questions carried forward</span></div><p>Observed outcome: Outcome not yet recorded. The meeting provides evidence of direction and commitments, but does not establish business impact.</p><details className="evidence"><summary>View evidence set</summary><p>All items remain traceable from the Meeting archive.</p></details></section></AppShell>; }
