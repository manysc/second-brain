import { AppShell } from "@/components/AppShell";
import { KnowledgeCard } from "@/components/KnowledgeCard";
import { getItems } from "@/lib/api";
export default async function Items({ searchParams }: { searchParams: Promise<{ type?: string }> }) { const allItems = await getItems(); const filter = (await searchParams).type; const items = filter ? allItems.filter((item) => item.type === filter) : allItems; return <AppShell><div className="page-head"><div><p className="eyebrow">Knowledge / Items</p><h1>Atomic knowledge</h1><p className="lede">Search and scan the facts that make the bigger picture possible.</p></div></div><div className="filter-row"><span className="filter active">All {allItems.length}</span><span className="filter">Ideas 2</span><span className="filter">Decisions 5</span><span className="filter">Actions 10</span><span className="filter">Questions 7</span></div><div className="card-grid">{items.map((item) => <KnowledgeCard key={item.id} item={item} />)}</div></AppShell>; }
