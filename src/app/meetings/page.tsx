import Link from "next/link";
import { AppShell } from "@/components/AppShell";
import { getMeeting } from "@/lib/api";
export default async function Meetings() { const meeting = await getMeeting(); const count = (type: string) => meeting.items.filter((item) => item.type === type).length; return <AppShell><div className="page-head"><div><p className="eyebrow">Knowledge / Meetings</p><h1>Meeting archive</h1><p className="lede">Every extracted signal, held close to its source.</p></div></div><section className="meeting-row"><div className="date-block"><strong>21</strong><span>AUG<br />2026</span></div><div className="meeting-main"><p className="eyebrow">{meeting.id}</p><h2>{meeting.title}</h2><p>One-on-one conversation with persistent threads across planning, releases, integration and performance.</p><div className="count-row"><span>{count("IDEA")} ideas</span><span>{count("DECISION")} decisions</span><span>{count("ACTION")} actions</span><span>{count("QUESTION")} questions</span><span>{meeting.reviewCandidates.length} review candidates</span></div></div><Link className="arrow-link" href={`/meetings/${meeting.id}`}>Open meeting <span>↗</span></Link></section></AppShell>; }
