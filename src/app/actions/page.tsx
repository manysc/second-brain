import { AppShell } from "@/components/AppShell";
import { KnowledgeCard } from "@/components/KnowledgeCard";
import { loadMeeting } from "@/lib/data";
export default function Actions() { const meeting = loadMeeting(); const items = meeting.items.filter((item) => item.type === "ACTION"); return <AppShell><div className="page-head"><div><p className="eyebrow">Knowledge / Actions</p><h1>Commitments across meetings</h1><p className="lede">The work that someone said would happen, with confidence and context intact.</p></div></div><div className="filter-row"><span className="filter active">All {items.length}</span><span className="filter">My actions</span><span className="filter">Overdue</span><span className="filter">Due soon</span><span className="filter">No due date</span></div><div className="card-grid page-cards">{items.map((item) => <KnowledgeCard key={item.id} item={item} />)}</div></AppShell>; }
